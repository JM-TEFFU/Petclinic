pipeline {
    agent { docker { image 'maven:3.9.9-eclipse-temurin-21-alpine' } }

    stages {
        stage('Check maven version') {
            steps {
                sh 'mvn --version'
            }
        }

        
        
    
    }
}
